"scripts": {
    "dev": "nodemon -r dotenv/config --experimental-json-modules index.js" 
    /* -r dotenv/config --experimental-json-modules (it is use to load environment variables from a .env file before running the application) */
  },